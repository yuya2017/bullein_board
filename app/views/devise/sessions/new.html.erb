 <ul class="cp_breadcrumb">
 	<li><a href="/">ホーム</a></li>
 	<li><a href="">ログイン</a></li>
 </ul>
 <% if notice %>
    <p class="alert alert-notice"><%= notice %></p>
  <% end %>
  <% if alert %>
    <p class="alert alert-error"><%= alert %></p>
  <% end %>
<div class="container">
  <div id="wrap" class="single">
    <h1 class="post-heading">ログイン</h1>
    <p class="login-title">登録がお済みのユーザー</p>
    <div class="login-group">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="login-form">
          <div class="field form-group login-content">
            <%= f.label :email, 'メールアドレス*' %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control' %>
          </div>
          <div class="field form-group login-content">
            <%= f.label :password, 'パスワード*' %><br />
            <%= f.password_field :password, autocomplete: "current-password", class: 'form-control' %>
          </div>
          <% if devise_mapping.rememberable? %>
            <div class="field login-content">
              <%= f.check_box :remember_me %>
              <%= f.label :remember_me %>
            </div>
          <% end %>
        </div>
        <div class="actions buttonContainer">
          <%= f.submit "ログイン", class: 'btn btn-primary buttonConfirm' %>
        </div>
      <% end %>
    </div>
    <%= render "devise/shared/links" %>
    <p class="login-title notlogin-text">まだご登録がお済みでないユーザー</p>
    <div class='notlogin-explanation'>
      <p>登録していただくと、投稿機能、マイページなどとて便利にお使いいただけます。</p>
      <p style="margin-bottom:0;">今回のみご利用のユーザーはゲストログインをお使いください。</p>
      <p style="color:#888;">※ゲストログインは投稿ができません。</p>
    </div>
    <div class="actions guestContainer">
      <%= link_to "新規登録", new_registration_path(resource_name), class: "btn buttonConfirm btn-info" %>
      <%= link_to 'ゲストログイン（閲覧用）', users_guest_sign_in_path, method: :post, class: "btn btn-warning buttonGuest" %>
    </div>
  </div>
</div>
